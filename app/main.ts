import { bootstrap }    from '@angular/platform-browser-dynamic';
import { ROUTER_PROVIDERS } from '@angular/router';

import { WebFieldForm } from './webff.component';

bootstrap(WebFieldForm, [ROUTER_PROVIDERS]);